import React from 'react';

const NotificationFail = (props) => {

        return (
            <div className="fail">
                {props.text}
            </div>
        );
    }
;

export default NotificationFail;
